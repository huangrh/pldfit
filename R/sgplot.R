#' Plot A Sensorgram For A kinfit Object
#'
#' Plot the predicted responses (nm) vs the Time (sec) variable at the corresponding
#' concentrations of ligand in solution. The fitted parameters kon, koff, rmax are
#' also presented on the plot.
#'
#' @export
#' @method plot kinfit
plot.kinfit <- function(object, ...) {
    # to do
    plot(1:10)

}
